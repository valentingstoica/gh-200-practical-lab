name: Hello GitHub Actions Lab

on:
  workflow_dispatch:

permissions: {}

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print a greeting
        run: echo "Hello from GitHub Actions!"
